import { VehicleFilterPipe } from './vehicle-filter.pipe';

describe('VehicleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
